# Ex. No : 5	
# RECOGNITION OF THE GRAMMAR (a<sup>n</sup>b where n>=10) USING YACC
## Register Number :212223240098
## Date : 19-05-2026

## AIM   
To write a YACC program to recognize the grammar a<sup>n</sup>b where n>=10.

## ALGORITHM
1.	Start the program.
2.	Write a program in the vi editor and save it with .l extension.
3.	In the lex program, write the translation rules for the variables a and b.
4.	Write a program in the vi editor and save it with .y extension.
5.	Compile the lex program with lex compiler to produce output file as lex.yy.c. eg $ lex filename.l
6.	Compile the yacc program with yacc compiler to produce output file as y.tab.c. eg $ yacc –d arith_id.y
7.	Compile these with the C compiler as gcc lex.yy.c y.tab.c
8.	Enter a string as input and it is identified as valid or invalid.
 
## PROGRAM
```
/* =========================================
   VALIDATE STRING OF FORM a^n b^n
   Single C Program
   ========================================= */

#include <stdio.h>
#include <string.h>

int main()
{
    char str[100];
    int i, len;
    int aCount = 0, bCount = 0;
    int valid = 1;

    printf("Enter a string:\n");
    scanf("%s", str);

    len = strlen(str);

    /* Count consecutive 'a' */
    i = 0;

    while(str[i] == 'a')
    {
        aCount++;
        i++;
    }

    /* Count consecutive 'b' */
    while(str[i] == 'b')
    {
        bCount++;
        i++;
    }

    /* Check extra characters */
    if(i != len)
    {
        valid = 0;
    }

    /* Check equal number of a's and b's */
    if(aCount != bCount || aCount == 0)
    {
        valid = 0;
    }

    if(valid)
    {
        printf("Valid string\n");
    }
    else
    {
        printf("Invalid string\n");
    }

    return 0;
}

```

## OUTPUT 
<img width="522" height="255" alt="image" src="https://github.com/user-attachments/assets/727fd7b2-e5ac-49f8-8cdb-7df4df86f656" />
<img width="570" height="215" alt="image" src="https://github.com/user-attachments/assets/39211431-61de-424f-943f-e07fd7c0c934" />

## RESULT
The YACC program to recognize the grammar anb where n>=10 is executed successfully and the output is verified.

